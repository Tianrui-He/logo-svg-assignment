Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 256". No prose, no markdown, no code fences.

User:
Create a single <svg ...>...</svg> element with a xmlns and viewBox="0 0 256 25